class TranslationService {
  // Placeholder: call Google Cloud Translation API from a secure backend.
  Future<String> translate({
    required String text,
    required String sourceLang,
    required String targetLang,
  }) async {
    // Return input for now to keep UI flow testable without keys.
    return text;
  }
}
